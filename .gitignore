# ── Secrets and local config ───────────────────────────────
backend/.env
backend/.env.*
!backend/.env.sample
frontend/legacy/config.js
frontend/**/config.local.js

# ── Python ─────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyd
.python-version
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Node / frontend build artifacts (for upcoming redesign) ─
node_modules/
frontend/**/dist/
frontend/**/.next/
frontend/**/.turbo/
frontend/**/.vite/

# ── App data (SQLite, persist across deploys) ──────────────
backend/data/
*.db
*.db-journal

# ── Database dumps (never commit — contain production data) ─
*.dump
*.sql.gz
*.sql.bz2
*.backup

# ── Logs ───────────────────────────────────────────────────
*.log

# ── Editors and OS ─────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
bitrix-sync/.env
backend/.env
Lidlar.xls

# local data exports
leadlar.xls
todaylead.csv
*.xls
